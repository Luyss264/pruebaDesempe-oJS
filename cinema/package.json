{
  "name": "cinema-spa",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "api": "json-server --watch db.json --port 3001",
    "start": "concurrently \"npm run dev\" \"npm run api\""
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "json-server": "^0.17.4"
  }
}
